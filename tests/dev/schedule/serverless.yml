service: dev-schedule

plugins:
  - ./../../../

provider:
  memorySize: 128
  name: aws
  region: us-east-1 # default
  runtime: nodejs12.x
  stage: dev
  versionFunctions: false

functions:
  hello:
    events:
      - schedule: rate(1 minute)
    handler: handler.hello
