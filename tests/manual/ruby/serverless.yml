service: aws-ruby

configValidationMode: error

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: ruby2.7

functions:
  hello:
    events:
      - http:
          method: get
          path: hello
    handler: handler.hello
