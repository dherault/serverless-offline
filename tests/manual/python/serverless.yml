service: aws-python

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: python2.7

functions:
  hello:
    events:
      - http:
          method: get
          path: hello
    handler: handler.hello
