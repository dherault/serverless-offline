# Welcome to Serverless!
#
# This file is the main config file for your service.
# It's very minimal at this point and uses default values.
# You can always add more config options for more control.
# We've included some commented out config examples here.
# Just uncomment any of them to get that config option.
#
# For full config options, check the docs:
#    docs.serverless.com
#
# Happy Coding!

service: manual-test-websocket-main

provider:
  name: aws
  runtime: nodejs10.x

  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:DescribeTable
        - dynamodb:Query
        - dynamodb:Scan
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
      Resource: 
        - 'Fn::ImportValue': DataTableForTesting
    - Effect: Allow
      Action:
        - "execute-api:ManageConnections"
      Resource:
        - "arn:aws:execute-api:*:*:**/@connections/*"

functions:
  connect:
    handler: config/handler-main.connect
    events:
      - websocket:
          route: $connect
  disconnect:
    handler: config/handler-main.disconnect
    events:
      - websocket:
          route: $disconnect
  default:
    handler: config/handler-main.defaultHandler
    events:
      - websocket:
          route: $default
  getClientInfo:
    handler: config/handler-main.getClientInfo
    events:
      - websocket:
          route: getClientInfo
  getCallInfo:
    handler: config/handler-main.getCallInfo
    events:
      - websocket:
          route: getCallInfo
  makeError:
    handler: config/handler-main.makeError
    events:
      - websocket:
          route: makeError
  doNotAnswerAsync:
    handler: config/handler-main.doNotAnswerAsync
    events:
      - websocket:
          route: doNotAnswerAsync
  replyViaCallback:
    handler: config/handler-main.replyViaCallback
    events:
      - websocket:
          route: replyViaCallback
  replyErrorViaCallback:
    handler: config/handler-main.replyErrorViaCallback
    events:
      - websocket:
          route: replyErrorViaCallback
  multiCall1:
    handler: config/handler-main.multiCall1
    events:
      - websocket:
          route: makeMultiCalls 
  multiCall2:
    handler: config/handler-main.multiCall2
    events:
      - websocket:
          route: makeMultiCalls      
  send:
    handler: config/handler-main.send
    events:
      - websocket:
          route: send
  registerListener:
    handler: config/handler-main.registerListener
    events:
      - websocket:
          route: registerListener
  deleteListener:
    handler: config/handler-main.deleteListener
    events:
      - websocket:
          route: deleteListener

plugins:
  - ./../../../..

package:
  excludeDevDependencies: false
  exclude:
    - ./**
  include:
    - config/handler-main.js

custom:
  dynamodb:
    stages:
      - dev