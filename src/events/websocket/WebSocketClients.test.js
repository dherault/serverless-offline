// import jest from 'jest'
// import WebSocketClients from './WebSocketClients.js'

describe('WebSocketClients class', () => {
  describe('_processEvent', () => {
    describe('when the route doesn’t match any function key', () => {
      test('thing', () => {
        // const client = {
        //   send: jest.fn()
        // }
        expect(true).toEqual(false)
      })
    })
  })
})
